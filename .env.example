# yappfy Environment
# Kopiere: cp .env.example .env

# Domain (localhost für Test, deinedomain.de für Produktion)
DOMAIN=localhost

# Datenbank-Passwort (wird beim ersten Setup generiert)
DB_PASSWORD=changeme

# Client-Port
ELEMENT_PORT=8080
